{
    "name": "lpos-market-testnet",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ hsxyl/lpos-market-testnet",
        "create-local": "graph create --node http://localhost:8020/ hsxyl/lpos-market-testnet",
        "remove-local": "graph remove --node http://localhost:8020/ hsxyl/lpos-market-testnet",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hsxyl/lpos-market-testnet",
        "test": "graph test"
    },
    "devDependencies": {
        "@graphprotocol/graph-cli": "0.35.0",
        "@graphprotocol/graph-ts": "0.28.1"
    },
    "dependencies": {
        "assemblyscript-json": "^1.1.0"
    }
}